version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
    env_file:
      - .env
    environment:
      - APP_NAME=${APP_NAME:-My Organization}
    restart: always
